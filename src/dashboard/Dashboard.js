import * as React from 'react';
import { Card, CardContent, CardHeader} from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title = "Welcome to Gurkha Connect"/>
        <CardContent>This is your main Dashboard!</CardContent>
    </Card>
)